<div class="select-cat">
  <form action="/" method="get">
    <!-- <label>Catégories -->
      <select id="select" name="categorie">
        <option value="" disabled selected>Sélectionnez une catégorie</option>
        <option value="enfant">Enfant</option>
        <option value="adulte">Adulte</option>
        <option value="cuisine">Cuisine</option>
        <option value="bien-être">Bien-être</option>
        <option value="apprentissage">apprentissage</option>
        <option value="maison">maison</option>
        <option value="voyage">voyage</option>
        <option value="jeux">jeux</option>
        <option value="jeu de plein air">jeu de plein air</option>
      </select>
    <!-- </label> -->
    <!-- <label>Budget Minimum -->

    <!--   <select name="budgetMin">
        <option value="" disabled selected>Sélectionnez un prix minimum</option>
        <% for (var i = 10; i <=30; i+= 10) { %>
          <option
           value="<%= i %>"
           <%= budgetMinPreviouslySelected == i ? "selected" : "" %>
           >
           <%= i %>€
          </option>
        <% } %>
      </select> -->

    <!-- </label> -->
    <!-- <label>Budget Maximum -->

<!--         <select name="budgetMax">
          <option value="" disabled selected>Sélectionnez un prix maximum</option>
          <% for (var i = 10; i <= 40; i+=10) { %>
              <option
                value="<%= i %>"
                <%= budgetMaxPreviouslySelected == i ? "selected" : "" %>
              >
                <%= i %>€
              </option>
          <% } %>
       </select> -->

       <script>
          $( function() {
            $( "#slider-range" ).slider({
              range: true,
              min: 0,
              max: 200,
              values: [ 0, 200 ],
              slide: function( event, ui ) {
                $( "#amount" ).val(ui.values[0] + " - ");
                $("#max").val(ui.values[1]) ;
                
              }
            });
            $( "#amount" ).val(  $( "#slider-range" ).slider( "values", 0 ))
            
            /* + $( "#slider-range" ).slider( "values", 1 ) ); */
            
            $( "#max" ).val(  $( "#slider-range" ).slider( "values", 1 ))
          } );
          </script>
         
         <p>
          <label>budget (minimum / maximum) :
            <input type="text" name="test" id="amount" readonly style="border:0; color:#f6931f; font-weight:bold;">
            <input type="text" id="max" readonly style="border:0; color:#f6931f; font-weight:bold;">
         </p>
            <div id="slider-range"></div>
        </label>
          
          <button type="submit">Rechercher</button>
        </form>
      </div>




<div class= "gift">
  <% if (allGifts.length > 0) { %>
   <% allGifts.forEach((gift) => { %>

    <div class="gift-display">
      <a href="/gifts/<%= gift._id %>">
    <h2>
      <%= gift.name %>
    </h2>
    <img src="<%= gift.imgPath %>" alt="gift picture" width="100%" />
    <p>
      <%= gift.price %> €
    </p>
  </div>
   <% }) %>
  <% } %>
</a>
</div>
