<h1>Panneau de contrôle Administrateur</h1>

<div class="admin-button">
  <form action="/admin/new" method="get">
    <button class="button is-info" type="submit">Ajouter un cadeau</button>
  </form>

  <form action="/" method="get">
    <button  class="button is-info" type="submit">Retour</button>
  </form>
</div>
<div>
  <ul>
    <% if (allGifts.length > 0) { %>
      <% allGifts.forEach((gift) => { %>
        <li>
          <a href="/admin/<%= gift._id %>/show"><%= gift.name %></a>
        </li>
      <% }) %>
    <% } %>
  </ul>
</div>
